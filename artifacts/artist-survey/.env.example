# Local dev: copy to `.env.local` and fill in the anon key (Settings → API → Project API keys → anon public).
# CI (GitHub Actions): set the same variables as repository secrets so the build embeds them.

VITE_SUPABASE_URL=https://mctbzidnaazmvxfgaspn.supabase.co
VITE_SUPABASE_ANON_KEY=
